name: webtufe_mobile
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.21+21

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  csv: ^5.0.2
  fl_chart: ^0.66.0
  http: ^1.1.0
  package_info_plus: ^8.0.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true 
  
  # CSV dosyaları için assets klasörü
  assets:
    - assets/ 